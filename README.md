# Git_commands
# Initialize repo
git init

# Add all files
git add .

# First commit
git commit -m "initial commit"

# Set main branch (optional but recommended)
git branch -M main

# Connect to GitHub repo (replace with your repo URL)
git remote add origin https://github.com/username/repo-name.git

# Push to GitHub
git push -u origin main
